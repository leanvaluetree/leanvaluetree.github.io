---
layout: layouts/base.njk
pagination:
  data: tools
  size: 1
  alias: tool
permalink: tools/{{ tool.url }}
---

<div class="container">
  <h2 class="tool-title">{{ tool["Nome tool"] }}</h2>
  <div class="contain">
    {{ tool['Descrizione di massima'] | safe }}

    <img class="tool-image" src="/images/{{tool.image}}" />

    {% properties tool %}

    <div class="divider"></div>

    {% usefulness tool %}
    
    <div class="divider"></div>

    {% todo tool %}

    <div class="divider"></div>
    
    {% info tool %}
  </div>
</div>

<div class="help-container tac" style="">
  <h3>Hai bisogno di aiuto?</h3>
  <a href="#" class="contact-experts uppercase">Contatta gli esperti</a>
</div>

{%- set nextTool = tools | next(tool) -%}
{%- set previousTool = tools | previous(tool) -%}

{% if nextTool %}
<div class="text-center py-5 bg">
  <h5 class="text-uppercase" style="font-size: 1rem">
    Prossimo workshop
  </h5>
  <div class="line-v"></div>
  <div class="marquee my-5">
    <div>
      <span class="text-uppercase">
        <a href="/tools{{nextTool.url}}">{{nextTool['Nome tool']}}</a>
      </span>
    </div>
  </div>
  <div class="line-v"></div>
  <h5 class="text-uppercase" style="font-size: 1rem">
    Scroll to discover
  </h5>
  <div class="nextpage-trigger py-3" data-url="/tools{{nextTool.url}}"></div>
</div>
{% endif %}
